services:
  - type: web
    name: oxlitica-backend
    runtime: python
    plan: starter
    buildCommand: pip install -e .
    startCommand: PYTHONPATH=src uvicorn oxler_risk_agent.api.app:build_app --factory --host 0.0.0.0 --port $PORT
    autoDeploy: true
